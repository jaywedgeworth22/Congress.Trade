# iOS agent build-loop policy (Congress.Trade)

Docs/hooks only.  `clients/ios/CLAUDE.md` is the annotated onboarding file.  `xcodebuild` via bash is pre-approved.  Do not stand up Xcode MCP.  Claude PreToolUse hook blocks `.pbxproj` / entitlements / xib writes.  New Swift files still need a human to add them to the Xcode target.

Canonical: `/Users/jay/apps/AGENT-SYNC.md` § iOS agent build loop.
