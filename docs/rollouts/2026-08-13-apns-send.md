# Congress.Trade APNs send

Register-only before (`push_devices`, `register_device`).  This adds HTTP/2 send for product events.

## What shipped

- `app/src/shared/apns.ts` — ES256 JWT + `node:http2`.  Tests inject a transport.
- `app/src/delivery/apnsFanout.ts` — official trades from `delivery_outbox` and unresolved `review_queue` rows, last 2 hours, collapse ids per tx/doc.
- Scheduled-tick lane `apns_fanout` after the delivery outbox.
- Migration `0086_apns_fanout_state` as a durable cursor fallback beside `CONFIG_KV`.

## Secrets (Infisical `congress-trade` prod)

| Name | Expected |
| --- | --- |
| `APNS_KEY_ID` | `P4US7YTWH4` (from `~/.secrets/CT_AuthKey_P4US7YTWH4.p8`, 257 bytes) |
| `APNS_TEAM_ID` | `CC8UTF7ATG` |
| `APNS_BUNDLE_ID` | `trade.congress.ios` |
| `APNS_P8` | PEM or base64 of that .p8 |
| `APNS_PRIVATE_KEY_B64` | same key, base64 |

Slice of #1046.  Tests never hit Apple.
