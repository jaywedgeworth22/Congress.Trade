# OS
.DS_Store

# Node
node_modules/
dist/
coverage/
.next/
out/
*.tsbuildinfo
.wrangler/

# Xcode user state
xcuserdata/
*.xcuserstate

# Retired 2026-08-11 rename leftover (period in the basename). The live
# project is clients/ios/CongressTrade.xcodeproj.
clients/ios/Congress.Trade.xcodeproj/

# Python / local watcher state
__pycache__/
.venv/
venv/
ptr_state/

# Secrets / local env
.dev.vars
.env
.env.local
*.local
*.env
!*.env.example
!*.env.sample
!*.env.template
.env.prod
prod.env

# Temp / scratch
*.tmp.json
*.log
.__probe
app/tsconfig.*.tmp.json
app/scripts/securities_master.sql
app/wrangler.preview.toml
.tmp-ocr/
scout/*.err
scout/*.log
scout/*state*.json

# Never commit real secrets (2026-08-01 purge; secrets live in Coolify env store)
app/.prod.vars.local
.prod.vars.local
.upstream-shared/

# Apple Auth Keys (APNs / DeviceCheck / Sign in with Apple) — canonical copy in ~/.secrets/
*.p8

# Merge/patch leftovers. app/docker-compose.yml.orig and two .patch files
# survived the sqlite-web fail-open fix still carrying
# SQLITE_WEB_PASSWORD:-admin, so applying one would have resurrected the
# default password for the production DB browser.
*.orig
*.rej

# Latency week tracker samples (may contain ops snapshots)
docs/latency-week/samples/

# Xcode local build output from clients/ios
clients/ios/build/
