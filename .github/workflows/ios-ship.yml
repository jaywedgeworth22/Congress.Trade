name: iOS TestFlight ship (Mac runner)

# Archive + upload on the owned Mac runner so TestFlight (the phone) actually
# updates when iOS code lands on main. Unsigned compile stays in ios-build.yml.
#
# XCODE BUILDS ONLY (owner 2026-08-12): this workflow may target mac-runner
# labels. Backend jobs stay off this host. Fork PRs never run here.
#
# Secrets stay on the Mac (~/.secrets/appstore-connect.env).
#
# NOT EVERY RUN SHIPS. The ship script rate-limits successful TestFlight
# uploads to one per app per DEFAULT_MIN_INTERVAL_SEC=9000 seconds (2.5 hours),
# a constant near the top of scripts/ios-fleet/ship-testflight.sh. Runs inside
# that window print "ship-gate: skip" and exit 0 (green, nothing uploaded); so
# do runs whose git HEAD already shipped. That gate is the main thing holding
# back automatic shipping. Neither kind of skip consumes a build number.
# To force one run, dispatch manually after exporting IOS_TF_MIN_INTERVAL_SEC,
# or run the wrapper locally with --force-ship. Changing the standing limit
# means editing DEFAULT_MIN_INTERVAL_SEC (owner's call, not the workflow's).

on:
  push:
    branches: [main]
    paths:
      - 'clients/ios/**'
      - '.github/workflows/ios-ship.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ios-ship-congress
  cancel-in-progress: false

jobs:
  ship:
    name: TestFlight archive + upload
    if: github.event.repository.fork == false
    runs-on: [self-hosted, macOS, ARM64, xcode26]
    timeout-minutes: 90
    env:
      DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Assert stable Xcode 26 (never beta)
        run: |
          echo "DEVELOPER_DIR=${DEVELOPER_DIR}"
          xcodebuild -version
          ver=$(xcodebuild -version | awk 'NR==1{print $2}')
          case "$ver" in
            26.*) echo "stable Xcode $ver — OK" ;;
            *)
              echo "::error::Expected stable Xcode 26.x, got '$ver'."
              exit 1
              ;;
          esac

      # The repo is the source of truth for the fleet tooling, but the LaunchAgent
      # path (ship-now-gui.sh -> /Users/jay/apps/ios-fleet/) runs the RUNTIME copy.
      # check-drift.sh exists to catch that copy going stale and, until now, had
      # ZERO callers -- so a fix could land in the repo while the owner's manual
      # "ship now" button kept running the old code. It did exactly that: the
      # runtime copy still carried both the seq-before-gate ordering bug and the
      # CFBundleVersion==MARKETING rejection trap after both were fixed here.
      # This job runs on the same Mac, so the runtime dir is present and drift fails.
      - name: Fail if the runtime ios-fleet copy has drifted from the repo
        run: bash "$GITHUB_WORKSPACE/scripts/ios-fleet/check-drift.sh"

      - name: Ship CongressTrade to TestFlight
        run: |
          # Wrapper resolves the in-repo fleet script (scripts/ios-fleet/) and
          # enforces the stable-Xcode rule; it falls back to the host copy only
          # if the repo copy is missing, and logs loudly when it does.
          bash "$GITHUB_WORKSPACE/scripts/ios-ship-testflight.sh"
