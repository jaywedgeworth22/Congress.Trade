# Core (CPU-only, Linux ARM64 + amd64) — pinned to avoid pip ResolutionImpossible on Coolify
numpy==2.2.6
pillow==11.2.1
opencv-python-headless==4.11.0.86
pytesseract==0.3.13
packaging>=24.0
# PDF render uses system poppler-utils (pdftoppm), not a Python dep
