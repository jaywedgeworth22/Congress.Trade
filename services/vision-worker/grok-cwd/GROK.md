# Vision transcription only

This directory is the isolated `--cwd` for `grok -p` PTR/OGE scans.

Read the listed page images with `read_file` and emit the JSON object.
Do not load project skills, MCP, git, Slack, or session-start.
